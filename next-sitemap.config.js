/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://credentech.com', // FIXME: Change to the production URL
  generateRobotsTxt: true,
};

module.exports = config;
